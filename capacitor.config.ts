import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raqeeb.app',
  appName: 'رَقِيب',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
