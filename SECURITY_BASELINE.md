# SECURITY BASELINE — التقييم الأمني الأساسي لمشروع رَقِيب

تاريخ التقييم: 2026-09-08
المراجع: الفريق الهندسي لمشروع رَقِيب

---

### 1. تدقيق المفاتيح السرية (Secret Audit)
* **مفتاح Gemini API**:
  * **التقييم**: **آمن تماماً (SECURE)**.
  * لا يوجد أي ذكر للمفتاح في كود الواجهة الأمامية (`src/`).
  * يتم استدعاء المفتاح حصرياً في خادم Node.js الخلفي (`server.ts`) عبر `process.env.GEMINI_API_KEY`.
  * يتم الاتصال بالذكاء الاصطناعي عبر مسار داخلي آمن `/api/chat`.
* **مفاتيح Firebase**:
  * المعرفات الموجودة في `firebase-applet-config.json` هي معرفات عميل Firebase العامة الضرورية للاتصال بالخدمة في تطبيقات الويب، ولا تحتوي على `serviceAccountKey` أو أسرار المشرف (Admin SDK).
* **مفاتيح إشعارات الويب (VAPID Keys)**:
  * يتم توليدها في الذاكرة على الخادم، ولا يتم تسريب المفتاح الخاص (Private Key) للمتصفح.

---

### 2. تدقيق قواعد أمان Firebase Firestore (`firestore.rules`)
* **التقييم**: **مشددة وآمنة (SECURE)**.
* **حظر الوصول العام**:
  ```javascript
  match /{document=**} { 
    allow read, write: if false; 
  }
  ```
* **حماية جلسات الاقتران (`pairingSessions/{sessionId}`)**:
  * لا يُسمح بإدراج جميع المستندات (`allow list: if false;`) لمنع كشف رموز الأجهزة الأخرى.
  * يُسمح فقط بالقراءة المباشرة بمعرفة رمز الاقتران الدقيق (`isValidId(sessionId)`).
  * عمليات الإنشاء والتحديث مقيدة بالتحقق من الحقول الإلزامية (`isValidPairingSession`).
  * حذف الجلسات محظور (`allow delete: if false;`).

---

### 3. تدقيق معالجة البيانات الحساسة والحافظة (Clipboard & Privacy)
* **الحافظة المشتركة (Shared Clipboard)**:
  * يتم تخزين النص المشترك فقط أثناء وجود جلسة اقتران نشطة بين جهازي المستخدم.
  * لا يتم تسجيل كلمات المرور أو أرقام البطاقات في أي سجلات دائمة (No permanent audit logging of sensitive data).
* **التخزين المحلي (LocalStorage)**:
  * البيانات المخزنة محلياً تقتصر على:
    1. إحصائيات اليوم (عدد مرات الاستغفار، صفحات القرآن، دقائق التركيز).
    2. تفضيلات الحماية (المواقع المحظورة، خيار الصوت).
    3. رمز الاقتران الخاص بجهاز المستخدم.
  * يتم تغليف عمليات القراءة والكتابة داخل `try/catch` لمنع انهيار التطبيق في حال تلاعب المستخدم بالبيانات المخزنة.

---

### 4. التوصيات الأمنية للمراحل القادمة (Security Hardening):
1. **تثبيت مفاتيح VAPID**: نقل مفاتيح VAPID الخاصة بـ Web Push إلى متغيرات بيئة ثابتة في `.env` بدلاً من إعادة توليدها عند إعادة تشغيل الخادم.
2. **تحديد معدل الطلبات (Rate Limiting)**: إضافة `express-rate-limit` على مسار `/api/chat` ومسار الاقتران `/api/phonelink/command` لمنع هجمات التخمين (Brute-force) على رموز الاقتران.
3. **تطهير مدخلات النصوص (Input Sanitization)**: التحقق الصارم من مدخلات البحث والنطاقات في الواجهة الأمامية والخلفية.
